name: CI
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: ./process
        steps:
            - uses: actions/checkout@v2
            - name: Install modules
              run: |
                npm i
                apt-get install libhunspell
            - name: Run tests
              run: npm test
